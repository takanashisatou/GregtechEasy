# GTE-Multi 贡献指南 / Contribution Guide

[🇨🇳 简体中文](#-简体中文) • [🌐 English](#-english)

---

## 🇨🇳 简体中文

欢迎来到 **GTE (GregTech Easy)** 整合包项目！我们致力于构建简单、好玩、有趣、耗时短的现代化 Minecraft 1.20.1 模组与整合包体验。

### 🤖 1. AI 工具与外部贡献政策 (AI-Friendly Policy)

- **零工具限制**：本项目**完全开放并全面拥抱现代 AI 辅助开发**！外部贡献者可以使用**任何 AI 工具或编码助手**（包括但不限于 Claude Code、Cursor、OpenAI Codex、Gemini、Antigravity、DeepSeek、Copilot、Aider 等）或纯手工编写代码。
- **唯一检验准绳：CI 自动化门禁 (CI as Ground Truth)**：
  无论代码由人类手写还是 AI 辅助生成，PR 能够合并的唯一标准是：**必须 100% 通过 GTE 的全套自动化 CI Action 检查**：
  - ✅ **编译严格通过**：开启 `-Werror` 零编译错误与警告；
  - ✅ **GameTest 实机自动化测试通过**：`:modules:gtm-reborn:runGameTestServer` 核心断言全部绿灯；
  - ✅ **遵守五大防崩溃铁律**：严禁强转 Mixin Accessor 接口，必须使用 `instanceof` 模式匹配守卫；
  - ✅ **子模块合法性**：子模块指针必须指向官方远程分支上的合法提交。
- **🏛️ 多方块建筑与创意思查机制 (Aesthetic & Balance Curation)**：
  我们非常欢迎由人类或 AI 设计的新型多方块机器与建筑结构！但除了 CI 自动化测试外，维护团队仍会**手动审查多方块结构的外观造型、几何美感、搭建手感以及玩法平衡性**，防止建筑过于突兀或破坏整体游玩体验。

---

### 🛡️ 2. 五大防崩溃开发铁律 (CRITICAL)

1. **严禁强转 Mixin Accessor 接口**：
   - ❌ 错误：`((BlockPropertiesAccessor) props).getDestroyTime()`（早期类加载必崩 `ClassCastException`）
   - ✅ 正确：`if (props instanceof BlockPropertiesAccessor acc) { ... }`
2. **禁止将生产环境优化/着色器 Mod 放入开发环境**：
   - 生产环境优化模组（`Oculus`, `Embeddium`, `ModernFix`, `ModernUI`）含有 SRG 混淆映射，严禁放入 `modules/gte-dev-runtime`；只允许放入 `gte/overrides/mods/`。
3. **开发环境依赖统一使用 `modLocalRuntime`**：
   - 普通 `localRuntime` 不会触发反混淆重映射器（Remapper）。
4. **子模块独立性保持**：
   - 对机器与核心逻辑的修改必须限制在拥有该特性的子模块内（`gtm-reborn`、`gtecore`、`gt--`）。
5. **美术资源分离**：
   - 原创 3D 模型与材质原件放入 `art_assets/`，通过 `syncBlockbenchAssets` 任务自动路由同步。

---

### 🛠️ 3. 提交 PR 标准流程

1. Fork 对应的子仓库或主仓库，克隆到本地（推荐带 `--recurse-submodules`）；
2. 确保本地环境为 **JDK 21**（`C:\Users\Ex_Je\.jdks\ms-21.0.11` 或系统的 JDK 21）；
3. 本地编译与测试：
   ```powershell
   .\gradlew.bat compileJava
   .\gradlew.bat :modules:gtm-reborn:runGameTestServer
   ```
4. 提交 PR 并等待 GitHub Actions 自动化 CI 变绿，代码所有者（Code Owner）审查后即可合并！

---

## 🌐 English

Welcome to the **GTE-Multi (GregTech Easy)** project! We are building a modern Minecraft 1.20.1 modpack and mod ecosystem focused on industrial aesthetics, billion-level overclock scaling, and developer ergonomics.

### 🤖 1. AI-Friendly Contribution Policy

- **Zero Tool Restriction**: This project **fully embraces modern AI-assisted software engineering**! Contributors may use **any AI coding agent or assistant** (including but not limited to Claude Code, Cursor, OpenAI Codex, Gemini, Antigravity, DeepSeek, GitHub Copilot, Aider, etc.) or write code purely by hand.
- **Single Ground Truth: Automated CI Gatekeeper**:
  Whether code is authored by a human or generated by an AI, the sole criterion for merging is: **It must 100% pass the full GTE automated GitHub Actions CI suite**:
  - ✅ **Strict Clean Compilation**: Compiles under `-Werror` with zero compiler errors;
  - ✅ **GameTest Integration Tests**: Headless server tests (`:modules:gtm-reborn:runGameTestServer`) must be green;
  - ✅ **Five Anti-Crash Rules**: Never force-cast Mixin accessors; use `instanceof` pattern matching guards;
  - ✅ **Submodule Lineage**: Submodule pointers must point to verified commits on official tracking branches.
- **🏛️ Multiblock Architecture & Balance Curation**:
  We warmly welcome creative multiblock structures and machine proposals from both human designers and AI tools! In addition to automated CI checks, maintainers will **manually review multiblock structural aesthetics, visual geometry, build feel, and gameplay balance** to ensure high design quality and avoid visual clutter or balance disruption.

---

### 🛡️ 2. Five Golden Anti-Crash Rules

1. **Never Force-Cast Mixin Accessor Interfaces**:
   - ❌ WRONG: `((BlockPropertiesAccessor) props).getDestroyTime()`
   - ✅ CORRECT: `if (props instanceof BlockPropertiesAccessor acc) { ... }`
2. **Never Put Production Shader/Optimizer Jars into Dev Runtime**:
   - Production optimizer mods (`Oculus`, `Embeddium`, `ModernFix`, `ModernUI`) contain SRG refmaps that break deobfuscated dev runs.
3. **Use `modLocalRuntime` for Dev Runtime Dependencies**:
   - Ensures ModDevGradle deobfuscation remapping is properly applied.
4. **Preserve Submodule Scopes**:
   - Keep edits scoped to the owning submodule (`gtm-reborn`, `gtecore`, `gt--`).
5. **Art Assets Decoupling**:
   - Raw Blockbench `.bbmodel` and textures belong in `art_assets/` and auto-route via `syncBlockbenchAssets`.

---

### 🛠️ 3. Pull Request Workflow

1. Fork the target submodule or root repository;
2. Verify local environment uses **JDK 21**;
3. Run local builds and tests:
   ```powershell
   .\gradlew.bat compileJava
   .\gradlew.bat :modules:gtm-reborn:runGameTestServer
   ```
4. Submit PR, ensure CI checks pass green, and wait for Code Owner review!
